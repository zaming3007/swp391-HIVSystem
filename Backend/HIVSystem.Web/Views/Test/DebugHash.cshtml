@{
    ViewData["Title"] = "Debug Hash Algorithms";
}

<div class="container">
    <h2><i class="fas fa-bug"></i> Debug Hash Algorithms</h2>
    
    <div class="alert alert-info">
        <h5>🔍 Tại sao phải cập nhật hash mỗi lần?</h5>
        <p>Có thể do hash algorithm không nhất quán. Trang này sẽ giúp tìm ra algorithm đúng.</p>
    </div>

    <div class="row">
        <div class="col-md-6">
            <div class="card">
                <div class="card-header">
                    <h4>🧪 Test Hash Algorithms</h4>
                </div>
                <div class="card-body">
                    <p><strong>Test Password:</strong> <code>@ViewBag.TestPassword</code></p>
                    
                    @if (ViewBag.Results != null)
                    {
                        <div class="table-responsive">
                            <table class="table table-sm">
                                <thead>
                                    <tr>
                                        <th>Algorithm</th>
                                        <th>Hash Result</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    @foreach (var result in (List<dynamic>)ViewBag.Results)
                                    {
                                        <tr>
                                            <td><strong>@result.Algorithm</strong></td>
                                            <td style="font-family: monospace; font-size: 11px; word-break: break-all;">
                                                @result.Hash
                                            </td>
                                        </tr>
                                    }
                                </tbody>
                            </table>
                        </div>
                    }
                </div>
            </div>
        </div>

        <div class="col-md-6">
            <div class="card">
                <div class="card-header">
                    <h4>🔍 Compare với Database</h4>
                </div>
                <div class="card-body">
                    <form method="get" action="/Test/CompareHashes">
                        <div class="mb-3">
                            <label class="form-label">Username để kiểm tra:</label>
                            <select name="username" class="form-control">
                                <option value="admin">admin</option>
                                <option value="doctor">doctor</option>
                                <option value="patient">patient</option>
                            </select>
                        </div>
                        <button type="submit" class="btn btn-primary">
                            <i class="fas fa-search"></i> So sánh Hash
                        </button>
                    </form>
                </div>
            </div>

            <div class="card mt-3">
                <div class="card-header">
                    <h4>🛠️ Quick Actions</h4>
                </div>
                <div class="card-body">
                    <a href="/Test/CompareHashes?username=admin" class="btn btn-info w-100 mb-2">
                        <i class="fas fa-user-shield"></i> Check Admin Hash
                    </a>
                    <a href="/Test/FixPasswordHashes" class="btn btn-warning w-100 mb-2">
                        <i class="fas fa-wrench"></i> Fix All Passwords
                    </a>
                    <a href="/Test/Database" class="btn btn-secondary w-100">
                        <i class="fas fa-database"></i> Back to Database
                    </a>
                </div>
            </div>
        </div>
    </div>
</div> 