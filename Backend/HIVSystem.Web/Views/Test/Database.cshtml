@{
    ViewData["Title"] = "Database Test";
}

<div class="container">
    <div class="d-flex justify-content-between align-items-center mb-3">
        <h2><i class="fas fa-database"></i> Database Connection Test</h2>
        <div>
            <a asp-action="UserStats" class="btn btn-success">
                <i class="fas fa-chart-bar"></i> Thống kê Users
            </a>
            <a asp-controller="Account" asp-action="Register" class="btn btn-primary">
                <i class="fas fa-user-plus"></i> Đăng ký User
            </a>
        </div>
    </div>
    
    @if (!string.IsNullOrEmpty(ViewBag.Error))
    {
        <div class="alert alert-danger">
            <h4>❌ Lỗi kết nối database:</h4>
            <p>@ViewBag.Error</p>
            <div class="mt-3">
                <a href="/Test/TestAllConnections" class="btn btn-warning">
                    <i class="fas fa-search"></i> Test tất cả connection strings
                </a>
            </div>
        </div>
    }
    
    @if (!string.IsNullOrEmpty(ViewBag.Message))
    {
        <div class="alert alert-success">
            <p>@Html.Raw(ViewBag.Message)</p>
        </div>
    }

    @if (TempData["Error"] != null)
    {
        <div class="alert alert-danger">
            <p>@TempData["Error"]</p>
        </div>
    }

    @if (TempData["Message"] != null)
    {
        <div class="alert alert-success">
            <p>@Html.Raw(TempData["Message"])</p>
        </div>
    }

    <div class="row">
        <div class="col-md-6">
            <div class="card">
                <div class="card-header">
                    <h4>📊 Thông tin kết nối</h4>
                </div>
                <div class="card-body">
                    <p><strong>Kết nối database:</strong> 
                        @if (ViewBag.CanConnect == true)
                        {
                            <span class="badge bg-success">✅ Thành công</span>
                        }
                        else
                        {
                            <span class="badge bg-danger">❌ Thất bại</span>
                        }
                    </p>
                    <p><strong>Số lượng Users:</strong> <span class="badge bg-info">@ViewBag.UserCount</span></p>
                    <p><strong>Bảng Users tồn tại:</strong> 
                        @if (ViewBag.TableExists == true)
                        {
                            <span class="badge bg-success">✅ Có</span>
                        }
                        else
                        {
                            <span class="badge bg-warning">⚠️ Không</span>
                        }
                    </p>
                </div>
            </div>
        </div>
        
        <div class="col-md-6">
            <div class="card">
                <div class="card-header">
                    <h4>🔧 Thao tác Database</h4>
                </div>
                <div class="card-body">
                    <div class="row">
                        <div class="col-md-6">
                            <a href="/Test/UserStats" class="btn btn-success mb-2 w-100">
                                <i class="fas fa-chart-bar"></i> Thống kê Users
                            </a>
                            <a href="/Account/Register" class="btn btn-primary mb-2 w-100">
                                <i class="fas fa-user-plus"></i> Đăng ký User mới
                            </a>
                            <a href="/Test/ConvertToPlainText" class="btn btn-warning mb-2 w-100">
                                <i class="fas fa-unlock"></i> Convert Plain Text
                            </a>
                            <a href="/Test/CreatePlainTextUsers" class="btn btn-info mb-2 w-100">
                                <i class="fas fa-users-cog"></i> Tạo Users Test
                            </a>
                            <a href="/Account/Login" class="btn btn-success mb-2 w-100">
                                <i class="fas fa-sign-in-alt"></i> Test Đăng nhập
                            </a>
                        </div>
                        <div class="col-md-6">
                            <a href="/Test/TestAllConnections" class="btn btn-secondary mb-2 w-100">
                                <i class="fas fa-search"></i> Test Connections
                            </a>
                            <a href="/Test/DebugHash" class="btn btn-secondary mb-2 w-100">
                                <i class="fas fa-bug"></i> Debug Hash
                            </a>
                            <a href="/Test/TestLogin" class="btn btn-secondary mb-2 w-100">
                                <i class="fas fa-vial"></i> Test Login Debug
                            </a>
                            <a href="/Test/FixPasswordHashes" class="btn btn-secondary mb-2 w-100">
                                <i class="fas fa-wrench"></i> Fix Hashes
                            </a>
                            <a href="/Test/CreateTestUser" class="btn btn-secondary mb-2 w-100">
                                <i class="fas fa-user-plus"></i> Tạo Test User
                            </a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    @if (ViewBag.Users != null)
    {
        <div class="mt-4">
            <div class="card">
                <div class="card-header d-flex justify-content-between align-items-center">
                    <h4>👥 Danh sách Users trong database</h4>
                    <a href="/Test/UserStats" class="btn btn-sm btn-outline-primary">
                        <i class="fas fa-chart-pie"></i> Xem thống kê chi tiết
                    </a>
                </div>
                <div class="card-body">
                    @if (((List<HIVHealthcareSystem.Models.User>)ViewBag.Users).Count == 0)
                    {
                        <div class="alert alert-warning">
                            <p>⚠️ Không có user nào trong database. Hãy tạo user test để có thể đăng nhập.</p>
                            <div class="mt-2">
                                <a href="/Test/CreatePlainTextUsers" class="btn btn-primary me-2">
                                    <i class="fas fa-users-cog"></i> Tạo Users Test
                                </a>
                                <a href="/Account/Register" class="btn btn-success">
                                    <i class="fas fa-user-plus"></i> Đăng ký User mới
                                </a>
                            </div>
                        </div>
                    }
                    else
                    {
                        <div class="table-responsive">
                            <table class="table table-striped">
                                <thead>
                                    <tr>
                                        <th>ID</th>
                                        <th>Username</th>
                                        <th>Full Name</th>
                                        <th>Email</th>
                                        <th>Active</th>
                                        <th>Created Date</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    @foreach (var user in (List<HIVHealthcareSystem.Models.User>)ViewBag.Users)
                                    {
                                        <tr>
                                            <td>@user.UserID</td>
                                            <td><code>@(user.Username ?? "N/A")</code></td>
                                            <td>@(user.FullName ?? "N/A")</td>
                                            <td>@(user.Email ?? "N/A")</td>
                                            <td>
                                                @if (user.IsActive)
                                                {
                                                    <span class="badge bg-success">Active</span>
                                                }
                                                else
                                                {
                                                    <span class="badge bg-danger">Inactive</span>
                                                }
                                            </td>
                                            <td>@user.CreatedDate.ToString("dd/MM/yyyy HH:mm")</td>
                                        </tr>
                                    }
                                </tbody>
                            </table>
                        </div>
                        
                        <div class="mt-3 text-center">
                            <small class="text-muted">
                                Hiển thị tối đa 10 users. 
                                <a href="/Test/UserStats">Xem thống kê đầy đủ →</a>
                            </small>
                        </div>
                    }
                </div>
            </div>
        </div>
    }

    <div class="mt-4">
        <div class="alert alert-info">
            <h5>📝 Hướng dẫn sử dụng:</h5>
            <div class="row">
                <div class="col-md-6">
                    <strong>Nếu gặp lỗi kết nối:</strong>
                    <ol>
                        <li>Click "Test Connections" để tìm connection hoạt động</li>
                        <li>Kiểm tra SQL Server có đang chạy không</li>
                        <li>Nếu database chưa tồn tại, chạy script SQL tạo database</li>
                        <li>Thử tạo user test</li>
                    </ol>
                </div>
                <div class="col-md-6">
                    <strong>Tài khoản test sẵn có:</strong>
                    <ul>
                        <li><code>admin</code> / <code>admin123</code> (Quản trị viên)</li>
                        <li><code>doctor</code> / <code>doctor123</code> (Bác sĩ)</li>
                        <li><code>patient</code> / <code>patient123</code> (Bệnh nhân)</li>
                    </ul>
                    <a href="/Account/Register" class="btn btn-sm btn-primary">
                        <i class="fas fa-user-plus"></i> Đăng ký tài khoản mới
                    </a>
                </div>
            </div>
        </div>
    </div>
</div> 